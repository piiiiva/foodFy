{% extends 'layout.njk' %}

{% block content %}
    <div class="all-content">
        <main>
            <div class="main-content">
                <h1>As melhores receitas</h1>
                <p>Aprenda a construir os melhores pratos com receitas criadas
                    por profissionais do mundo inteiro.
                </p>
            </div>
            <div class="main-image">
                <img src="/assets/chef.png" alt="Foodfy-cheff-image">
            </div>
            
        </main>
        
        <span>Mais acessadas</span>
    
        <section class="cards">
        {% for recipe in recipes %}
            <div class="card">
                <div class="card-image">
                    <img src="{{recipe.image}}" alt="Imagem de {{recipe.title}}">
                </div>
                <div class="card-info">
                    <p class="recipe-name">{{recipe.title}}</p>
                    <p class="recipe-author">por {{recipe.author}}</p>
                </div>
            </div> 
        {% endfor %}
                      
        </section>

    </div>
{% endblock content %}